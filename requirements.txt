# Python >= 3.8
# See README.md "Install Dependencies" for PyTorch / DGL GPU setup.

# PyTorch (install manually to match your CUDA version)
torch==2.1.0
torchvision==0.16.0
torchaudio==2.1.0

# PyTorch Geometric (install manually — wheel depends on torch + CUDA version)
torch-geometric==2.1.0
torch-scatter==2.1.2
torch-sparse==0.6.18

# DGL (install manually — wheel depends on torch + CUDA version, only needed for b4e/)
dgl==1.1.2

# Scientific computing
numpy==1.26.4
scipy==1.15.3

# Machine learning
scikit-learn==1.0.2

# Data processing
pandas==1.3.5

# Visualization (hyperparameter comparison plots)
matplotlib==3.7.5

# Utilities
tqdm==4.67.1
